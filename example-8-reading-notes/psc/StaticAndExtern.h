#include <stdio.h>

/* Declare a global variable. You need to define before using this variable
 * Generally, the extern keyword is not used in the source code */
extern int b;

/* Declare and define a static global variable
 * This variable can be accessed by all functions */
static int a = 85;
